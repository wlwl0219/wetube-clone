//- 만들어 놓은 main템플릿을 이용해서 block태그안에 
//- 알맞는 내용을 넣어 확장성있게 사용한다.

//- each in << 문을 사용하는데, for in 문이나, forEach와 상당히 유사함.
//- (배열 내 데이터를 반복해서 돌면서 모두 가져오는 것)

extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each item in videos
        
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            }) 