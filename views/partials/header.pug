//- header에서 라우트 객체에 접근이 가능하게 하기 위해 미들웨어를 사용해야한다.
header.header
    .header__column
        a(href=routes.home)
            i.fab.fa-youtube
    .header__column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Login